{
  "name": "sparklehood_assesment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "seed": "npx ts-node src/seed.ts",
    "dev": "npm run build && npm run seed && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@types/express": "^5.0.1",
    "express": "^5.1.0",
    "prisma": "^6.6.0",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/node": "^latest",
    "ts-node": "^latest",
    "typescript": "^latest"
  }
}